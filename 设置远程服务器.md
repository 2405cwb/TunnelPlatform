# 设置远程服务器

本文记录的是把 `TunnelPlatform.Api` 部署到阿里云 Ubuntu 服务器的完整流程。  
当前方案是：

- 应用本体监听 `127.0.0.1:5140`
- Nginx 对外监听 `80`，后续可接 `443`
- PostgreSQL 运行在服务器本机
- 静态页面和 API 由同一个 ASP.NET Core 程序提供

## 1. 端口规划

- `22`：SSH 远程登录
- `80`：HTTP 对外访问
- `443`：HTTPS，对接域名证书后使用
- `5140`：应用内部端口，仅本机访问，不对公网开放

推荐安全组规则：

- `22/22` 只放行你的办公公网 IP
- `80/80` 可先放行 `0.0.0.0/0`
- `443/443` 证书配置后再放行

## 2. 本地发布

在开发机上执行：

```powershell
dotnet publish .\TunnelPlatform.Api\TunnelPlatform.Api.csproj -c Release -r linux-x64 --self-contained false -o .\publish\api
```

如果服务器上没有 .NET 运行时，也可以改成 `--self-contained true`，但发布体积会更大。

## 3. 上传到服务器

先把发布目录传上去：

```powershell
scp -r ./publish/api root@<服务器公网IP>:/tmp/tunnel-api
```

说明：

- 这里传的是整个 `api` 目录
- 到服务器上后，文件会落在 `/tmp/tunnel-api/`

登录服务器后，把文件复制到正式目录：

```bash
mkdir -p /opt/tunnel-platform/api
cp -r /tmp/tunnel-api/* /opt/tunnel-platform/api/
mkdir -p /opt/tunnel-platform/server-storage
```

## 4. 安装 PostgreSQL

如果服务器还没装数据库：

```bash
sudo apt update
sudo apt install -y postgresql postgresql-contrib
```

进入 PostgreSQL：

```bash
sudo -u postgres psql
```

创建数据库和用户：

```sql
CREATE DATABASE tunnel_platform;
CREATE USER tunnel_user WITH PASSWORD '请替换为你的密码';
GRANT ALL PRIVILEGES ON DATABASE tunnel_platform TO tunnel_user;
\q
```

如果应用启动时要自动建库，用户还需要建库权限：

```sql
ALTER USER tunnel_user CREATEDB;
```

如果你不想给建库权限，也可以先手动建好数据库，再让应用只负责建表。

## 5. 安装 .NET 运行时

Ubuntu 22.04 需要先加 Microsoft 源，再装 .NET。

```bash
sudo apt update
sudo apt install -y wget gnupg apt-transport-https ca-certificates software-properties-common

wget https://packages.microsoft.com/config/ubuntu/22.04/packages-microsoft-prod.deb -O /tmp/packages-microsoft-prod.deb
sudo dpkg -i /tmp/packages-microsoft-prod.deb

sudo apt update
sudo add-apt-repository ppa:dotnet/backports
sudo apt update
sudo apt install -y aspnetcore-runtime-10.0
```

检查安装结果：

```bash
which dotnet
dotnet --info
```

## 6. 配置应用连接串

编辑服务器上的：

```bash
nano /opt/tunnel-platform/api/appsettings.json
```

建议内容：

```json
{
  "ConnectionStrings": {
    "DefaultConnection": "Host=localhost;Port=5432;Database=tunnel_platform;Username=tunnel_user;Password=请替换为你的密码"
  },
  "Storage": {
    "RootPath": "/opt/tunnel-platform/server-storage"
  },
  "AllowedHosts": "*"
}
```

注意：

- 不要把真实密码提交到公开仓库
- 生产环境更建议使用 `appsettings.Production.json` 或环境变量

## 7. systemd 服务

创建服务文件：

```bash
sudo nano /etc/systemd/system/tunnel-platform-api.service
```

内容如下：

```ini
[Unit]
Description=Tunnel Platform API
After=network.target postgresql.service

[Service]
WorkingDirectory=/opt/tunnel-platform/api
ExecStart=/usr/bin/dotnet /opt/tunnel-platform/api/TunnelPlatform.Api.dll
Restart=always
RestartSec=10
KillSignal=SIGINT
SyslogIdentifier=tunnel-platform-api
User=root
Environment=ASPNETCORE_ENVIRONMENT=Production
Environment=ASPNETCORE_URLS=http://127.0.0.1:5140

[Install]
WantedBy=multi-user.target
```

启动服务：

```bash
sudo systemctl daemon-reload
sudo systemctl enable tunnel-platform-api
sudo systemctl start tunnel-platform-api
sudo systemctl status tunnel-platform-api -l
```

看日志：

```bash
sudo journalctl -u tunnel-platform-api -f
```

## 8. Nginx 反向代理

安装 Nginx：

```bash
sudo apt update
sudo apt install -y nginx
```

创建站点：

```bash
sudo nano /etc/nginx/sites-available/tunnel-platform
```

配置示例：

```nginx
server {
    listen 80 default_server;
    listen [::]:80 default_server;
    server_name _;

    client_max_body_size 0;

    location / {
        proxy_pass http://127.0.0.1:5140;
        proxy_http_version 1.1;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
    }
}
```

启用并重载：

```bash
sudo rm -f /etc/nginx/sites-enabled/default
sudo ln -sf /etc/nginx/sites-available/tunnel-platform /etc/nginx/sites-enabled/tunnel-platform
sudo nginx -t
sudo systemctl reload nginx
sudo systemctl enable nginx
```

验证：

```bash
curl http://127.0.0.1:5140/
curl http://<服务器公网IP>/
```

## 9. 域名和 HTTPS

如果你不想一直用数字 IP，做法是：

1. 给域名加 `A` 记录，指向服务器公网 IP
2. 把 Nginx 的 `server_name` 改成你的域名
3. 用 Certbot 签证书

示例：

```nginx
server_name example.com www.example.com;
```

申请证书：

```bash
sudo apt install -y certbot python3-certbot-nginx
sudo certbot --nginx -d example.com -d www.example.com
```

## 10. 常见问题

### 10.1 `curl 127.0.0.1:5140` 连接拒绝

检查 API 服务是否启动：

```bash
sudo systemctl status tunnel-platform-api -l
sudo journalctl -u tunnel-platform-api -n 80 --no-pager
```

常见原因：

- PostgreSQL 没起来
- 连接串写错
- `tunnel_user` 没有权限
- 数据库不存在

### 10.2 公网打开的是 Nginx 欢迎页

通常是默认站点还在抢占 80 端口。  
先删除：

```bash
sudo rm -f /etc/nginx/sites-enabled/default
sudo systemctl reload nginx
```

### 10.3 本机 `localhost:5140` 和公网不是一回事

- `127.0.0.1:5140` 只表示当前机器自己
- 服务器上的 `127.0.0.1:5140` 是服务器自己
- 公网用户应该访问域名或服务器 IP 的 `80/443`

## 11. 当前部署结果

当前这台服务器的实际链路是：

```text
浏览器 -> http://43.106.8.3/ -> Nginx(80) -> 127.0.0.1:5140 -> ASP.NET Core API
```

如果后续要再挂别的项目：

- 每个程序用不同本地端口
- Nginx 用不同域名或不同 `server block` 区分
- 一台服务器可以同时托管多个站点

## 12. Ubuntu 常见目录速查

这是 Ubuntu 服务器里最常见的一些目录，以及通常放什么：

| 目录 | 含义 | 常放内容 |
|------|------|----------|
| `/` | 根目录 | 所有目录的起点 |
| `/root` | `root` 用户家目录 | 临时脚本、root 专用配置，不建议长期堆文件 |
| `/home` | 普通用户家目录集合 | 各用户自己的项目、文档、下载文件 |
| `/opt` | 额外安装的软件目录 | 自己部署的程序、第三方服务、业务系统 |
| `/var` | 可变数据目录 | 日志、缓存、数据库、队列、服务运行时数据 |
| `/var/log` | 日志目录 | Nginx、systemd、系统日志 |
| `/etc` | 系统配置目录 | Nginx 配置、systemd 服务、数据库配置 |
| `/usr` | 系统程序和共享资源 | 系统安装的软件、命令、库文件 |
| `/usr/bin` | 常用命令 | `dotnet`、`nginx`、`psql` 这类可执行程序 |
| `/usr/local` | 本机手动安装的软件 | 手工安装的工具、脚本、二进制 |
| `/tmp` | 临时目录 | 临时上传包、解压文件、测试文件，系统可能清理 |
| `/srv` | 服务数据目录 | 某些服务的数据根目录，适合放对外服务内容 |
| `/var/www` | Web 站点目录 | 静态网页、传统站点文件 |
| `/bin` | 基础命令 | `ls`、`cat`、`cp` 这类基础工具 |
| `/sbin` | 系统管理命令 | 网络、磁盘、系统维护命令 |
| `/lib` / `/lib64` | 系统库文件 | 命令运行所需动态库 |
| `/mnt` | 临时挂载点 | 手动挂载磁盘、U 盘、云盘 |
| `/media` | 可移动设备挂载点 | U 盘、移动硬盘自动挂载位置 |

## 13. 这次项目里实际用到的目录

结合你现在这套部署，最关键的是这几个：

- `/opt/tunnel-platform/api`：发布后的 API 程序
- `/opt/tunnel-platform/server-storage`：业务文件存储根目录
- `/etc/systemd/system/tunnel-platform-api.service`：API 的 systemd 服务
- `/etc/nginx/sites-available/tunnel-platform`：Nginx 站点配置
- `/etc/nginx/sites-enabled/`：启用中的站点链接
- `/var/log/nginx/`：Nginx 日志
- `/opt/tunnel-platform/api/logs/`：API 自己写的日志

## 14. 记法

你可以简单记成：

- ` /etc ` 放配置
- ` /var ` 放运行中的动态数据和日志
- ` /opt ` 放你自己部署的程序
- ` /home ` 放用户自己的文件
- ` /tmp ` 放临时东西

